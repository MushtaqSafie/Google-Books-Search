import React from "react";
import { Col, Row, Container } from "../components/Grid";

const Saved = () => {
  return (
    <Container fluid>
      <Row>
        <Col size="md-12">
          <h1>Saved</h1>
        </Col>
      </Row>
    </Container>
  );
};

export default Saved;
